# requirements.txt
# Instalar con: pip install -r requirements.txt
#
# VERSIONES FIJADAS CON == A PROPOSITO
# ------------------------------------
# El pipeline es determinista: mismo dataset.xlsx + SEMILLA=42 producen
# exactamente los mismos splits (verificado por hash MD5 de train_X.npy).
# Pero esa garantia SOLO se sostiene si todo el equipo usa las mismas
# versiones de libreria: un cambio de version en scikit-learn puede alterar
# el orden de categorias del OneHotEncoder o el generador aleatorio, y ahi
# los .npy dejarian de coincidir y los modelos ya no serian comparables.
#
# Estas son las versiones con las que se entrenaron todos los modelos de
# entrenamiento_v1 y entrenamiento_v2.
# Python 3.13.3

# --- Nucleo: usado por TODOS los scripts ---
pandas==2.3.1
numpy==2.3.2
scikit-learn==1.8.0
joblib==1.5.2
openpyxl==3.1.5          # lectura de los .xlsx
matplotlib==3.10.7       # graficos de diagnostico

# --- CatBoost ---
catboost==1.2.10

# --- MLP / red neuronal ---
# El MLP principal usa sklearn.neural_network.MLPClassifier (ya viene en
# scikit-learn, no requiere nada extra).
#
# TensorFlow hace falta SOLO para el Experimento 3 de
# entrenamiento_v2/06_modelo_mlp_v2.py: entrenar la MISMA arquitectura en
# Keras para medir si el resultado depende de la libreria o de los
# hiperparametros. Si no lo instalas, el script corre igual y salta ese
# experimento (esta envuelto en un try/ImportError).
#
# OJO CON LA VERSION: tensorflow 2.18 NO existe para Python 3.13
# (el soporte de 3.13 llego en 2.20). Pesa ~600 MB.
tensorflow==2.20.0       # arrastra keras 3.15.1

# --- XGBoost ---
xgboost==3.4.1

# --- Aplicacion final (Etapa 9, aun no empezada) ---
# streamlit==1.41.1

# --- ELIMINADAS del proyecto original y por que ---
# imbalanced-learn : traia SMOTE, que fabrica victimas sinteticas interpolando
#                    registros reales. Se descarto por el criterio de datos
#                    reales del proyecto.
# seaborn          : matplotlib cubre todos los graficos que necesitamos.
# shap             : los modelos ya exponen coef_ / feature_importances_,
#                    que son interpretables sin libreria extra.
